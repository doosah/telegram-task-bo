# 🤖 АВТОМАТИЧЕСКАЯ НАСТРОЙКА - ВСЁ САМО!

Этот файл содержит команды, которые вы просто копируете и вставляете.

---

## ✅ ШАГ 1: ЗАГРУЗКА НА GITHUB (АВТОМАТИЧЕСКИ)

### Вариант А: Через скрипт (САМЫЙ ПРОСТОЙ!)

1. Найдите файл `ЗАГРУЗИТЬ_НА_GITHUB.ps1` в папке с ботом
2. Нажмите на него **правой кнопкой мыши**
3. Выберите **"Выполнить с помощью PowerShell"**
4. Следуйте инструкциям на экране

### Вариант Б: Через команды (если скрипт не работает)

Откройте PowerShell (Win+R → введите `powershell` → Enter) и выполните команды:

```powershell
# Переходим в папку с ботом
cd C:\Users\Ноут\Downloads\SBRouting\telegram-task-bot

# Инициализируем Git
git init

# Добавляем все файлы
git add .

# Сохраняем
git commit -m "Загрузка бота"

# Подключаем к GitHub (ЗАМЕНИТЕ YOUR_USERNAME на ваше имя!)
git remote add origin https://github.com/YOUR_USERNAME/telegram-task-bot.git

# Загружаем
git branch -M main
git push -u origin main
```

**Важно:** При запросе пароля используйте **Personal Access Token** (см. ниже)

---

## 🔑 КАК ПОЛУЧИТЬ PERSONAL ACCESS TOKEN (для GitHub):

1. Откройте https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните:
   - **Note:** `Railway Bot`
   - **Expiration:** `90 days` (или `No expiration`)
   - Поставьте галочку **"repo"** (все пункты внутри)
4. Нажмите **"Generate token"** внизу
5. **СКОПИРУЙТЕ ТОКЕН СРАЗУ!** Он больше не будет показан
6. Используйте этот токен как пароль при загрузке файлов

---

## ✅ ШАГ 2: НАСТРОЙКА RAILWAY

### 2.1. Войдите в Railway

1. Откройте https://railway.app
2. Нажмите **"Login"** → **"Login with GitHub"**

### 2.2. Создайте проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `telegram-task-bot`

### 2.3. Добавьте переменные

В Railway откройте проект → вкладка **"Variables"** → **"+ New Variable"**

**Добавьте 3 переменные:**

```
Имя: BOT_TOKEN
Значение: 8448041977:AAGa4-EZ9dTfn-GgYArZU83FteWfisBOEUo
```

```
Имя: CHAT_ID
Значение: -1003107822060
```

```
Имя: ADMIN_USERNAME
Значение: Doosahyasno
```

### 2.4. Настройте запуск

В Railway → **"Settings"** → **"Start Command"** → введите: `python bot.py` → **"Save"**

### 2.5. Проверьте статус

В Railway → **"Deployments"** → дождитесь зелёной галочки (Success)

---

## ✅ ШАГ 3: ПРОВЕРКА

1. Откройте Telegram
2. Найдите вашего бота
3. Отправьте `/start`
4. Если бот ответил - всё работает! 🎉

---

## 📋 ЧЕКЛИСТ (отметьте галочками):

- [ ] Файлы загружены на GitHub
- [ ] Проект создан в Railway
- [ ] Добавлена переменная BOT_TOKEN
- [ ] Добавлена переменная CHAT_ID
- [ ] Добавлена переменная ADMIN_USERNAME
- [ ] Указана команда запуска: python bot.py
- [ ] Статус в Railway: Success (зелёная галочка)
- [ ] Бот отвечает на /start в Telegram

**Если все галочки поставлены - бот работает! 🚀**

---

## 🆘 ЕСЛИ ЧТО-ТО НЕ РАБОТАЕТ:

### Бот не отвечает:
1. Проверьте логи в Railway → Deployments → Logs
2. Убедитесь, что все 3 переменные добавлены
3. Проверьте, что бот в групповом чате

### Ошибка в логах:
1. Откройте Railway → Deployments → Logs
2. Найдите красные строки
3. Скопируйте текст ошибки
4. Проверьте переменные (без лишних пробелов!)

### Бот не отправляет в группу:
1. Убедитесь, что бот добавлен в групповой чат
2. Убедитесь, что у бота есть права администратора
3. Проверьте CHAT_ID (должен начинаться с минуса!)

---

**Всё готово! Бот будет работать автоматически! 🎉**

